ANTHROPIC_API_KEY=sk-ant-...
SESSION_SECRET=change-this-to-a-long-random-string
PORT=3000
MAIAR_ROOT=/Users/yourname/maiar-server
NODE_ENV=development

# Optional: share the admin workspace with Claude Code. Point this at the Maiar project
# root so files created in Claude Code (drafts/, context/, etc.) appear instantly in the
# web UI Library — and edits made in the UI are visible to Claude Code. Same directory, both ways.
# ADMIN_WORKSPACE=/Users/yourname/Documents/Maiar

# Password reset email (Google Workspace — use App Password, not account password)
# Generate App Password: Google Account → Security → 2-Step Verification → App Passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=admin@maiar.work
SMTP_PASS=your-google-app-password
SMTP_FROM=admin@maiar.work
APP_URL=http://localhost:5173

# Email deliverability checklist:
# 1. Use Google Workspace with an App Password (not personal Gmail — personal has strict sending limits)
# 2. Ensure your domain has SPF record: v=spf1 include:_spf.google.com ~all
# 3. Enable DKIM signing in Google Workspace Admin → Apps → Gmail → Authenticate email
# 4. Add a DMARC record: v=DMARC1; p=quarantine; rua=mailto:admin@yourdomain.com
# Without SPF+DKIM+DMARC, reset emails may land in spam.
