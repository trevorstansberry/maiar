ANTHROPIC_API_KEY=sk-ant-...
SESSION_SECRET=change-this-to-a-long-random-string
PORT=3000
MAIAR_ROOT=/Users/yourname/maiar-server
NODE_ENV=development

# Optional: share the admin workspace with Claude Code. Point this at the Maiar project
# root so files created in Claude Code (drafts/, context/, etc.) appear instantly in the
# web UI Library — and edits made in the UI are visible to Claude Code. Same directory, both ways.
# ADMIN_WORKSPACE=/Users/yourname/Documents/Maiar

# Resend — transactional email for password resets
# Get your API key at https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxx
# Sender address — must be from a verified domain in Resend (https://resend.com/domains)
EMAIL_FROM=Maiar <noreply@maiar.work>
APP_URL=http://localhost:5173
