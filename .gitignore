# Credentials — never commit
.env

# Work in progress (team can remove these lines to share drafts via git)
drafts/
published/

# Client workspaces — contain workspace-specific context and content
clients/

# System files
.DS_Store
node_modules/
